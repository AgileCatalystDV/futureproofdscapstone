# Core dependencies
pandas>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# PandaAI
pandasai>=2.0.0

# OpenAI (for GPT-4 mini)
openai>=1.0.0

# Slack SDK
slack-sdk>=3.20.0
slack-bolt>=1.18.0

# MCP (Model Context Protocol) - for DatabaseToolbox integration
# Note: MCP Python SDK is not yet on PyPI
# Install from GitHub: pip install git+https://github.com/modelcontextprotocol/python-sdk.git
# Or use MCP DatabaseToolbox binary directly (recommended)
# The code handles missing MCP gracefully with ImportError fallback

# Database (for real Postgres later)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Development
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

